import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-auth-demo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
